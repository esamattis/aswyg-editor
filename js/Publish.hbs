
<h1>
Publish?
</h1>

<div class="content">
    <p>
    {{#if unpublishedChanges}}
        {{#if publishDone}}

        Page is now public at
        <a href="{{publicUrl}}" target="_new">{{publicUrl}}</a>

        {{else}}

        <a href="#" class="publish" >Yes!</a>

        {{/if}}

    {{else}}

        This page has no unpublished changed.

    {{/if}}
    </p>
</div>
